---
title: 潜伏者
date: 2019-07-03 09:05
update: 2019-07-03 09:05
author: Rainboy
---

## 解析

模拟

## 代码

```c
/*-----------------
* author: Rainboy
* email: rainboylvx@qq.com
* time: 2019年 05月 08日 星期三 10:59:32 CST
* problem: luogu-1071
*----------------*/
#include <cstdio>
#include <cstring>
#include <algorithm>
using namespace std;

char s1[105];
char s2[105];
char s3[105];

char _map[400]={0};
char _map2[400]={0};

int main(){
    scanf("%s",s1);
    if( strlen(s1) <26){
        printf("Failed");
        return 0;
    }
    
    scanf("%s",s2);
    scanf("%s",s3);
    int len = strlen(s1);
    int i;
    for( i= 0;i<len;i++){
        char u  = s1[i];
        char v  = s2[i];
        if( _map[u] == 0 && _map2[v] ==0){
            _map[u] = v;
            _map2[v] = u;
        }
        else if(_map[u] != 0 &&  _map[u] != v){
            printf("Failed");
            return 0;
        }
        else if( _map2[v] !=0 && _map2[v] != u){

            printf("Failed");
            return 0;
        }
    }
    for(i='A';i<='Z';i++)
    {
        if( _map[i] == 0){
            printf("Failed");
            return 0;
        }
    }
    len = strlen(s3);
    for (i=0;i<len;i++){
        printf("%c",_map[s3[i]]);
    }
    
    return 0;
}
```
